define([], function()
{
  describe('list', function()
  {

    describe('when app starts', function()
    {
      // Note(hbt) make it smart i.e display list based on geolocation, time (e.g morning list), default list, or last list
      xit('list tasks', function()
      {

      })
    });

  });
})


// sync, priority, auth, offline, checklist, process, do, modes